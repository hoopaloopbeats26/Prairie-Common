# Deployment target

Planned production domain: prairiecommon.ca
Hosting path: GitHub repository -> Cloudflare Workers static assets.

Do not point the production domain at this package until `site/` contains the recovered/verified approved public build.
